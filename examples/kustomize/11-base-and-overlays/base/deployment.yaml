apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout
spec:
  replicas: 1
  selector:
    matchLabels:
      component: checkout
  template:
    metadata:
      labels:
        component: checkout
    spec:
      containers:
      - name: app
        image: nginx:1.27-alpine
