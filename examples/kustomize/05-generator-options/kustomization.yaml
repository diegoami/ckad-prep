configMapGenerator:
- name: feature-flags
  literals:
  - CHECKOUT_V2=false
  - GIFT_WRAP=true
# Applies to every generator in this file.
generatorOptions:
  disableNameSuffixHash: true      # plain name "feature-flags", no hash suffix
  labels:
    team: storefront
  annotations:
    owner: platform-team
