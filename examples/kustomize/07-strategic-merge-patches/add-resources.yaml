apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-api
spec:
  template:
    spec:
      containers:
      - name: app                  # containers are merged by name
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            memory: 256Mi
