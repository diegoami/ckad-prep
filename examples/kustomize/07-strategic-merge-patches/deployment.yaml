apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-api
spec:
  replicas: 1
  selector:
    matchLabels:
      component: search-api
  template:
    metadata:
      labels:
        component: search-api
    spec:
      containers:
      - name: app
        image: nginx:1.27-alpine
