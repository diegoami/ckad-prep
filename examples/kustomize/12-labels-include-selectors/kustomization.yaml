resources:
- deployment.yaml
labels:
- pairs:
    app.kubernetes.io/name: reviews
  includeSelectors: true           # also added to spec.selector: fine for new objects,
                                   # but selectors are immutable on existing Deployments
- pairs:
    cost-center: books-42
  includeSelectors: false          # metadata only (the default)
