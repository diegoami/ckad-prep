apiVersion: apps/v1
kind: Deployment
metadata:
  name: reviews
spec:
  replicas: 1
  selector:
    matchLabels:
      component: reviews
  template:
    metadata:
      labels:
        component: reviews
    spec:
      containers:
      - name: app
        image: nginx:1.27-alpine
