# CKAD Workbook

> A blank copy of [drill.md](drill.md): same questions, same numbering, no answers.
> Write your answer in the empty block under each question, then check it against drill.md.
> Mark the ones you got wrong or hesitated on with an `x` and redo them next session.
>
> Generated by `python3 drills/build.py` — edit drill.md, not this file.

---


## 1. Core kubectl

### 1.1 Create an nginx pod in namespace `dev`, never restart

```bash

```

### 1.2 Generate the YAML for the pod above without creating it

```bash

```

### 1.3 Create a busybox pod that runs `env`, prints output, then auto-deletes

```bash

```

### 1.4 Get all pods across all namespaces, wide output

```bash

```

### 1.5 Change the image of pod `nginx` container `nginx` to `nginx:1.24.0`

```bash

```

### 1.6 Get the pod IP of `nginx` using jsonpath

```bash

```

### 1.7 Execute a shell inside a running pod `nginx`

```bash

```

### 1.8 Get logs from the previous (crashed) instance of pod `nginx`

```bash

```

### 1.9 Create a ResourceQuota `myrq` with 1 CPU, 1G memory, 2 pods max (dry run only)

```bash

```

### 1.10 Find where `livenessProbe` lives under a Deployment's spec, in one command

```bash

```

### 1.11 You remember a Pod spec has a `capabilities` field somewhere, but not its exact location — find it without opening the docs

```bash

```

### 1.12 You forgot the exact flag to roll back a Deployment to a specific revision — find it in 2 seconds without leaving the terminal

```bash

```

### 1.13 Create a ResourceQuota that caps *requests* at 1 CPU/1Gi and *limits* at 2 CPU/2Gi separately

```bash

```


## 2. Labels & Annotations

### 2.1 Create 3 nginx pods with label `app=v1` in one line

```bash

```

### 2.2 Show all pods with label columns `app` and `tier`

```bash

```

### 2.3 Change label `app=v1` to `app=v2` on pod `nginx2`

```bash

```

### 2.4 Get pods where `app=v2` but NOT `tier=frontend`

```bash

```

### 2.5 Add label `tier=web` to all pods with `app=v1` or `app=v2`

```bash

```

### 2.6 Remove the `app` label from pods `nginx1 nginx2 nginx3`

```bash

```

### 2.7 Annotate pods `nginx1 nginx2` with `owner=marketing`

```bash

```


## 3. Deployments

### 3.1 Create a deployment `web` with image `nginx:1.18.0`, 3 replicas, port 80

```bash

```

### 3.2 Update the image to `nginx:1.19.8`

```bash

```

### 3.3 Check rollout status, then see history

```bash

```

### 3.4 Undo the last rollout

```bash

```

### 3.5 Roll back to revision 2 specifically

```bash

```

### 3.6 Pause the rollout, change image, resume

```bash

```

### 3.7 Autoscale `web` between 3 and 8 pods, CPU target 75%

```bash

```

### 3.8 Write the YAML for a canary setup: 3 replicas of v1, 1 replica of v2, one Service selecting both via shared label `app: myapp`

```bash

```

### 3.9 Weighted traffic split across two Deployments: create `blue` (10 replicas, label `tier=web`) and `green` (10 replicas, label `tier=web`). Service `bg-svc` selects on `tier=web`. Scale to 70% blue, 30% green

```bash

```

### 3.10 Change env var `TIER=web` to `TIER=app` on deployment `nginx-deployment`

```bash

```

### 3.11 Patch deployment `my-deployment` to set `revisionHistoryLimit: 20` using a strategic merge patch

```bash

```

### 3.12 Write the full YAML for a DaemonSet `node-logger` using `busybox` that runs `sleep 3600` on every node

```bash

```

### 3.13 What DaemonSet update strategy lets you update pods manually node-by-node? How do you set it?

```bash

```

### 3.14 Write the full YAML for a ReplicaSet `frontend`: 3 replicas of `nginx`, matching label `app=frontend`

```bash

```

### 3.15 Scale ReplicaSet `frontend` directly to 5 replicas, then delete one of its pods by hand — what happens?

```bash

```

### 3.16 Check exactly what image revision 2 of Deployment `revtest` used, without diffing YAML by hand

```bash

```

### 3.17 Deployment `web` reads a ConfigMap that you just changed. Recreate all its pods, one rolling step at a time, without editing the Deployment's YAML

```bash

```


## 4. Jobs & CronJobs

### 4.1 Create a job `pi` using `perl:5.34` that prints pi to 2000 digits

```bash

```

### 4.2 Wait for job `pi` to complete, then get its logs

```bash

```

### 4.3 Create a job that runs busybox 5 times sequentially (completions)

```bash

```

### 4.4 Same job, but run 5 in parallel

```bash

```

### 4.5 Kill the job automatically if it takes more than 30 seconds

```bash

```

### 4.6 Create a CronJob `heartbeat` that runs every minute printing the date

```bash

```

### 4.7 CronJob should be skipped if it hasn't started within 17 seconds of its scheduled time. What field?

```bash

```

### 4.8 Manually trigger a job from a CronJob named `heartbeat`

```bash

```

### 4.9 Bonus: three ways to delete completed Jobs

```bash

```

### 4.10 Write a Job `pi` that gives up after 6 failed attempts, and is force-killed if still running after 10 seconds

```bash

```

### 4.11 What does `completionMode: Indexed` change about a Job, versus the default?

```bash

```

### 4.12 CronJob `pi` should never run two instances at once — if the previous run hasn't finished when the next is due, skip it

```bash

```

### 4.13 Keep only the last 1 failed Job and last 3 successful Jobs in CronJob `pi`'s history

```bash

```

### 4.14 Translate these cron schedules: `*/15 * * * *`, `0 2 * * *`, `0 9 * * 1-5`

```bash

```


## 5. Multi-container Pods

### 5.1 Write the YAML section to add a second container `busybox2` to an existing pod spec

```bash

```

### 5.2 Exec into container `busybox2` inside pod `mypod`

```bash

```

### 5.3 Write the full YAML for a pod with an init container that writes `"ready"` to `/shared/status`, and a main nginx container that mounts the same volume at `/usr/share/nginx/html`

```bash

```

### 5.4 Two containers, `server` (nginx) and `client` (busybox), sit in the same Pod. How does `client` reach `server` — does it need a Service?

```bash

```

### 5.5 What is a sidecar container? Write a Pod `web` where nginx writes its access log to a shared volume and a classic sidecar `log-tailer` (busybox) streams that log to stdout

```bash

```

### 5.6 Rewrite 5.5's `log-tailer` as a native sidecar. What makes it one, and what changes?

```bash

```


## 6. Configuration

### 6.1 Create a ConfigMap `app-config` with `ENV=prod` and `LOG_LEVEL=info`

```bash

```

### 6.2 Create a ConfigMap from a file `config.txt`, key named `settings`

```bash

```

### 6.3 Write the pod spec snippet to consume ConfigMap key `ENV` as env var `APP_ENV`

```bash

```

### 6.4 Write the pod spec snippet to mount ConfigMap `app-config` as a volume at `/etc/config`

```bash

```

### 6.5 Create a Secret `app-secret` with `password=changeme-42`

```bash

```

### 6.6 Decode the value of key `password` from secret `app-secret`

```bash

```

### 6.7 Write the pod spec snippet to consume Secret key `password` as env var `DB_PASSWORD`

```bash

```

### 6.8 Mount secret `app-secret` as a volume at `/etc/secrets` (read-only)

```bash

```

### 6.9 Write the pod spec snippet to add Linux capabilities `NET_ADMIN` and `SYS_TIME` to a container

```bash

```

### 6.10 Create a LimitRange in namespace `dev` that sets max pod memory to 500Mi and min to 100Mi

```bash

```

### 6.11 Create a ServiceAccount `deployer`, then assign it to an nginx pod

```bash

```

### 6.12 Write pod spec snippet to run container as user `1000` and enforce non-root

```bash

```

### 6.13 Write pod spec snippet: read-only root filesystem, no privilege escalation

```bash

```

### 6.14 What is the difference between pod-level and container-level `securityContext`? Give an example of each

```bash

```

### 6.15 Generate an API token for service account `myuser`

```bash

```

### 6.16 Create a pod that does NOT automount its service account token

```bash

```

### 6.17 Create a Secret for pulling images from a private registry given credentials directly (not a podman/docker auth file), and use it on a Pod

```bash

```

### 6.18 Create a ConfigMap from a `KEY=VALUE`-per-line file, one ConfigMap key per line

```bash

```


## 7. Observability

### 7.1 Write a liveness probe that runs `ls` every 5 seconds, starting after 5 seconds

```bash

```

### 7.2 Write an HTTP readiness probe on path `/healthz` port `8080`

```bash

```

### 7.3 Write a TCP liveness probe on port `3306`

```bash

```

### 7.4 Pod `app` crashed on startup — no logs show. What do you do?

```bash

```

### 7.5 Find pods with liveness probe failures in the current namespace

```bash

```

### 7.6 List all pods cluster-wide sorted by CPU, then separately sorted by memory

```bash

```

### 7.7 Get all events across all namespaces sorted by creation timestamp

```bash

```

### 7.8 Get logs from pod `log-pod` for the last hour

```bash

```

### 7.9 Convert an Ingress manifest from `networking.k8s.io/v1beta1` to `networking.k8s.io/v1`

```bash

```

### 7.10 Find all events related to Deployment `web` in namespace `dev`, oldest first

```bash

```

### 7.11 Delete a pod that's stuck `Terminating` and won't go away

```bash

```

### 7.12 A legacy app can take up to 5 minutes to start, and its liveness probe (`httpGet /healthz:8080`, every 10s) keeps killing it during startup. Fix it without making the liveness probe slower

```bash

```

### 7.13 Pod `api` runs a distroless image with no shell. Attach a busybox shell that can see the `api` container's processes, without restarting the pod

```bash

```

### 7.14 Pod `api` is in `CrashLoopBackOff`, so there's nothing running to attach to. Debug it on a copy of the pod instead, with a busybox container added and the process namespace shared

```bash

```


## 8. Services & Networking

### 8.1 Create an nginx pod on port 80, and expose it as a ClusterIP service in one command

```bash

```

### 8.2 Expose deployment `web` on service port 8080, target port 3000

```bash

```

### 8.3 Convert service `nginx` from ClusterIP to NodePort imperatively

```bash

```

### 8.4 Write a NetworkPolicy that allows only pods with label `role=frontend` to access pods with label `app=db` on port 5432

```bash

```

### 8.5 Test connectivity: hit service `mysvc` on port 80 from a temporary busybox pod

```bash

```

### 8.6 Write an Ingress `my-ingress` routing all traffic at path `/` to service `my-service` on port 8080

```bash

```

### 8.7 Write a NetworkPolicy that denies ALL ingress traffic to pods with label `app=web`

```bash

```

### 8.8 Write a NetworkPolicy allowing ingress to `app=api` on port 80 only from pods in namespaces labeled `env=prod`

```bash

```

### 8.9 Write a NetworkPolicy that denies ALL egress from pods with label `app=restricted`

```bash

```

### 8.10 Write two NetworkPolicies: `tier=web` pods can reach `tier=app` pods on port 80 (egress), and `tier=app` pods only accept from `tier=web` on port 80 (ingress)

```bash

```

### 8.11 Create an Ingress `simple` imperatively: host `foo.com`, path `/bar`, routing to service `svc1:8080`

```bash

```

### 8.12 Create an Ingress `catch-all` with no host restriction, path `/path`, IngressClass `otherclass`

```bash

```

### 8.13 Create an Ingress `multipath` on host `foo.com` with two paths: `/` → `svc:port`, `/admin/` → `svcadmin:portadmin`

```bash

```

### 8.14 Create an Ingress `ingtls` for host `foo.com` path `/`, backend `svc:port`, with TLS terminated using existing secret `my-cert`

```bash

```

### 8.15 Generate the YAML for an Ingress (don't create it) routing `foo.com/*` to `svc:8080`, with a default backend of `defaultsvc:80`

```bash

```

### 8.16 A Deployment `web` in namespace `shop` has no Service. From another pod, what must exist before `http://web` resolves, and how do you verify it?

```bash

```

### 8.17 Write an Egress NetworkPolicy for `tier=app` pods: allow egress to `tier=db` pods on port 5432, and allow DNS resolution to anywhere

```bash

```

### 8.18 Write a NetworkPolicy allowing ALL ingress traffic to `app=web` pods (the opposite of 8.7's deny-all)

```bash

```

### 8.19 Write a NetworkPolicy allowing ingress to `app=db` only from pods labeled `role=frontend` **in namespaces labeled `env=prod`** (both conditions on the same source)

```bash

```

### 8.20 Write a NetworkPolicy allowing ingress to `app=db` from pods with `role=frontend`, but only within `db`'s own namespace (no cross-namespace access)

```bash

```

### 8.21 A Service remaps port 8001 → container port 8000. What port does a NetworkPolicy restricting traffic to that Service's pods need to reference?

```bash

```

### 8.22 Write a NetworkPolicy allowing ingress on port 80 to `app=web` pods from absolutely anywhere — internal pods and external LoadBalancer traffic alike

```bash

```

### 8.23 A Service "exists" but a client gets connection refused / times out reaching it — what's the standard first command to check, before touching DNS or the pods?

```bash

```

### 8.24 A NetworkPolicy already restricts ingress to `app=db` pods to only those labeled `role=frontend`. Pod `client` (no such label) can't connect, and the policy must not be touched — fix it

```bash

```


## 9. State / Persistence

### 9.1 Write the full YAML for a PersistentVolume `mypv`: 5Gi, ReadWriteOnce, hostPath `/data`, storageClass `manual`

```bash

```

### 9.2 Write the PVC that binds to the PV above: request 2Gi

```bash

```

### 9.3 Write the pod spec snippet to mount PVC `mypvc` at `/data`

```bash

```

### 9.4 Two pods both mount the same PVC using `ReadWriteOnce`. Pod 2 can't see Pod 1's files on a multi-node cluster. Why? Fix?

```bash

```


## 10. Helm

### 10.1 Add the Bitnami repo and install `bitnami/nginx` as release `web` with 3 replicas

```bash

```

### 10.2 Upgrade release `web` using a custom values file

```bash

```

### 10.3 List all Helm releases that are in a pending state across all namespaces

```bash

```

### 10.4 Uninstall release `web` from namespace `prod`

```bash

```

### 10.5 Download (not install) the `bitnami/redis` chart and untar it

```bash

```

### 10.6 Show the revision history of release `web`, then roll it back to revision 2

```bash

```

### 10.7 Show the values you set when installing release `web`, then every value the chart is actually using

```bash

```

### 10.8 Release `web` was installed with `--set replicaCount=3`. Upgrade it to set `image.tag=1.27` without losing the replica count

```bash

```

### 10.9 Find what chart versions of `bitnami/nginx` are available, then upgrade release `web` to version `15.5.7` specifically

```bash

```


## 11. CRD

### 11.1 Write the CRD manifest for a `Widget` resource in group `acme.io`, namespaced, with fields `color: string` and `size: integer`

```bash

```

### 11.2 Create a custom object of kind `Widget` named `my-widget` with `color: blue`, `size: 3`

```bash

```

### 11.3 List all widgets using the short name

```bash

```

### 11.4 What is an Operator? An operator was installed in your cluster — find which CRDs it added and what kinds they define, then create one of its custom resources

```bash

```


## 12. Podman

### 12.1 Build image `myapp` from the current directory

```bash

```

### 12.2 Run `myapp` as a container named `test`, mapping host port 8080 to container port 80, detached

```bash

```

### 12.3 Show the image layers of `myapp`

```bash

```

### 12.4 Export container `test` to `backup.tar`

```bash

```

### 12.5 Tag `myapp` for a local registry at `localhost:5000` and push it

```bash

```

### 12.6 Create a `kubernetes.io/dockerconfigjson` secret from existing podman login credentials

```bash

```

### 12.7 Build `myapp:v2` from the Dockerfile in the current directory, tag it, and save it to `/opt/backups/myapp.tar` in OCI format

```bash

```


## 13. yq — In-place YAML editing (optional, not in the curriculum)

### 13.1 Change the replica count in `deploy.yaml` to 5

```bash

```

### 13.2 Change the image of the first container in `pod.yaml` to `nginx:1.24`

```bash

```

### 13.3 Add label `env=prod` to the pod template in `deploy.yaml`

```bash

```

### 13.4 Set `activeDeadlineSeconds: 30` on `job.yaml`

```bash

```

### 13.5 Read the current image of the first container from `deploy.yaml` without opening the file

```bash

```

### 13.6 Full workflow: generate a Job YAML, set completions to 5, then apply

```bash

```


## 14. Cluster Introspection

### 14.1 Show the currently active namespace

```bash

```

### 14.2 List all resource types that exist inside a namespace (names only)

```bash

```

### 14.3 Set memory request 25Mi and limit 100Mi on all containers in deployment `my-deployment`

```bash

```

### 14.4 Switch the active namespace to `staging` without changing the context name

```bash

```

### 14.5 Show only the name of the currently active context

```bash

```

### 14.6 View the full merged kubeconfig, then a minified view of only the active context

```bash

```


## 15. RBAC

### 15.1 Create a Role `pod-reader` in namespace `dev` that allows `get`, `list`, `watch` on pods

```bash

```

### 15.2 Bind Role `pod-reader` to user `alice` in namespace `dev`

```bash

```

### 15.3 Create a ClusterRole `node-reader` that allows `get`, `list` on nodes

```bash

```

### 15.4 Bind ClusterRole `node-reader` to ServiceAccount `myuser` in namespace `dev`

```bash

```

### 15.5 Check if user `alice` can list pods in namespace `dev`

```bash

```

### 15.6 Check if ServiceAccount `myuser` in namespace `dev` can create deployments

```bash

```

### 15.7 Bind Role `pod-reader` to ServiceAccount `myuser` in namespace `dev`

```bash

```

### 15.8 A pod using ServiceAccount `myuser` logs `Error from server (Forbidden): pods is forbidden: User "system:serviceaccount:dev:myuser" cannot list resource "pods" in API group "" in the namespace "dev"`. Confirm the gap and fix it in place

```bash

```


## 16. Scheduling

### 16.1 Label node `node01` with `disktype=ssd`, then write a pod spec that uses `nodeSelector` to land only on it

```bash

```

### 16.2 Taint node `node01` with `tier=frontend:NoSchedule`, then write a pod spec that tolerates it

```bash

```

### 16.3 Write a pod with required node affinity: only schedule on nodes labeled `region=eu-west`

```bash

```

### 16.4 Write a pod with preferred node affinity: prefer nodes labeled `zone=az1` but don't require it

```bash

```

### 16.5 Remove the taint `tier=frontend:NoSchedule` from node `node01`

```bash

```


## 17. Kustomize

### 17.1 Apply all resources defined in a kustomization directory `./base`

```bash

```

### 17.2 Write a `kustomization.yaml` referencing `deploy.yaml` and `svc.yaml`, adding namePrefix `dev-` and label `env: dev` to all resources

```bash

```

### 17.3 Write a kustomization overlay that patches the replica count of deployment `web` to 5

```bash

```

### 17.4 Write a kustomization that generates a ConfigMap `app-config` from a `.env` file

```bash

```

### 17.5 Preview what kustomize would generate without applying it

```bash

```


## 18. `kubectl get` — Filtering & Output Flags

### 18.1 List only pods in phase `Running`, across all namespaces, using a field selector

```bash

```

### 18.2 Show every pod's labels as a column, without knowing the label keys in advance

```bash

```

### 18.3 List all pods across all namespaces, oldest first

```bash

```

### 18.4 List all pods sorted by restart count, so the pods restarting most are easy to find

```bash

```

### 18.5 Get a script-friendly, one-name-per-line list of pods with label `app=web`, no header row

```bash

```


## 19. `kubectl run` — Pod Creation Flags

### 19.1 Create pod `ann-test` from `nginx` with two annotations at creation time: `owner=alice` and `team=platform`

```bash

```

### 19.2 Create pod `cmd-test` from `busybox`, replacing its ENTRYPOINT entirely (not just CMD) with `/bin/sh -c "echo hi"`

```bash

```

### 19.3 Create pod `env-test` from `busybox` with two env vars baked in at creation: `FOO=bar`, `BAZ=qux`

```bash

```

### 19.4 Create pod `priv-test` from `busybox` running as a privileged container

```bash

```
